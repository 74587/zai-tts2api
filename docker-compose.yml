services:
  zai-tts:
    container_name: zai-tts
    image: ghcr.io/aahl/zai-tts:main
    build: .
    ports:
      - 8823:80
    environment:
      ZAI_USERID: ${ZAI_USERID}
      ZAI_TOKEN: ${ZAI_TOKEN}
      TZ: ${TZ:-Asia/Shanghai}
    restart: unless-stopped
